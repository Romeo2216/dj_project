services:
  web:
    build: ./web
    command: /app/entrypoint.sh
    volumes:
      - ./web:/app
      - static_volume:/app/staticfiles
      - sqlite_data:/data 
    env_file:
      - .env
    ports:
      - "80:80"

volumes:
  static_volume:
  sqlite_data:
